package com.example.fullstack;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Experiment 2.1.1 : RESTful API design (CRUD, validation, standardized responses, CORS)
 * Experiment 2.1.2 : Global exception handling, logging, correlation-ID request tracing
 */
@SpringBootApplication
public class FullstackApiApplication {
    public static void main(String[] args) {
        SpringApplication.run(FullstackApiApplication.class, args);
    }
}
